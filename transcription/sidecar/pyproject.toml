[project]
name = "digestor-transcribe"
version = "0.1.0"
description = "faster-whisper sidecar spawned by digestor's Deno transcription pipeline"
requires-python = ">=3.12,<3.13"
dependencies = ["faster-whisper>=1.1"]
